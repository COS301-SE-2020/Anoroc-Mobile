<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:local="clr-namespace:AnorocMobileApp.Models" xmlns:local1="clr-namespace:AnorocMobileApp.Views"
             mc:Ignorable="d"
             x:Class="AnorocMobileApp.Views.Login">

    
    <ContentPage.BindingContext>
        <local:LoginViewModel />
    </ContentPage.BindingContext>
    <StackLayout
        Orientation="Vertical"
        HorizontalOptions="CenterAndExpand"
        VerticalOptions="CenterAndExpand">
        
        <Label x:Name="login_heading" Text="Login"
                HorizontalOptions="CenterAndExpand" />
        <Button x:Name="google_login_button" Text="Login With Google" Clicked="loginGoogle"/>
        <Button Text="Sign Up" Clicked="btn_signup_Clicked"/>
        
        <Label
            Text="XF Facebook Login Example:"
            HorizontalTextAlignment="Center" />
        <local1:FacebookLoginButton
            OnSuccess="{Binding OnFacebookLoginSuccessCmd}"
            OnError="{Binding OnFacebookLoginErrorCmd}"
            OnCancel="{Binding OnFacebookLoginCancelCmd}" />
        <Button
            Text="FB Logout - backup"
            Command="{Binding FacebookLogoutCmd}" />
    </StackLayout>

</ContentPage>